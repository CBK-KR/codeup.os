a = int(input())
n = 0 # 1씩 더해줄 정수
s = 0 # 정수에 쌓아줄 총합계


while s<a: # n+s가 누적되면 언젠가 55가 넘는 순간이 옴 (아래코드 참조)
    n += 1 # s보다 a가 클 시 지속적으로 n을 1씩 증가
    s = n+s # n의 값인 1, 2, 3, 4... 등을 계속 더해주다보면 n= 10일 때 55가 됨.
    
print(n) # n의 숫자인 10을 출력
